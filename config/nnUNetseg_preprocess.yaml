# nnUNet plan & preprocess configuration
# Usage: python src/segmentation/nnunet_preprocess.py [--config config/nnUNetseg_preprocess.yaml]

task_id: "101"
task_name: "Meningioma"
config: "2d"
verify_dataset_integrity: true

# Paths — relative to project root, or set absolute paths
root_dir: "./datasets"
